@page "{id:int}"
@model ShoeTools.Website.Pages.ProductDetails.List

<div class="container-fluid">
    <form method="get">
        <div class="row">
            <div class="col-md-8">
                <input type="search" name="Id" value="@Model.Id"
                       readonly class="form-control" placeholder="Buscar por Id"/>
            </div>
            <div class="col-md-4">
                <a asp-page=".\Add"  asp-route-id="@Model.Id" class="btn btn-success">Add new</a>
            </div>
        </div>
    </form>
    
    <div class="row">
        <div class="col-md-1"></div>
        <div class="col-md-10">
            <table class="table">
                <tr>
                    <th>Id</th>
                    <th>Name</th>
                    <th>Model</th>
                    <th>Price</th>
                    <th>Size</th>
                    <th>Stock</th>
                </tr>
                
                @foreach (var product in @Model.productDetails)
                {
                    <tr>
                        <td>@product.Id</td>
                        <td>@Model.Product.Name</td>
                        <td>@product.Model</td>
                        <td>@product.Price</td>
                        <td>@product.Size</td>
                        <td>@product.Stock</td>
                        <td>
                            <a asp-page="/ProductDetails/Edit" asp-route-id="@product.Id"
                               class="btn btn-primary" role="button">Edit</a>
                            <a asp-page="/ProductDetails/Delete" asp-route-id="@product.Id"
                               class="btn btn-danger" role="button">Delete</a>
                        </td>
                    </tr>
                }
            </table>
        </div>
        <div class="col-md-1"></div>
    </div>
</div>